[core]
	editor = nvim
	excludesfile = ~/.gitignore
	pager = diff-so-fancy | less --tabs=1,5 -R
[user]
	name = Kevin Durbin
	email = kdurbin@churchcommunitybuilder.com
[color]
	diff = auto
	status = auto
	branch = auto
	ui = auto
[color "branch"]
	current = yellow reverse
	local = yellow 
	remote = green
[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold
[color "status"]
	added = green
	changed = red
	untracked = cyan
[alias]
	s = status
  aa = add --all
	st = status
	ci = commit
	br = branch
	co = checkout
	switch = checkout
	sw = checkout
  mk = checkout -b
	df = diff
	lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
	m = merge --no-commit
	info = config --list
	amend = commit --amend
	up = fetch
	d = difftool
	diff = diff -w
  pu = smart-pull
  puts = push origin
  putsup = push --set-upstream origin
  cp = cherry-pick
  a = amend
	out = fire
	appraise = !/Users/kdurbin/go/bin/git-appraise
[diff]
	tool = vimdiff
[push]
	default = tracking
[rerere]
	enabled = 1
[mergetool]
	keepBackup = true
	prompt = false
[difftool]
	prompt = true
[pull]
	rebase = true
[merge]
	tool = vimdiff
	conflictstyle = diff3
[color "diff-highlight"]
	oldNormal = red bold
	oldHighlight = red bold 52
	newNormal = green bold
	newHighlight = green bold 22
